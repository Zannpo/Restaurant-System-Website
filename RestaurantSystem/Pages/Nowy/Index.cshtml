@page
@model RestaurantSystem.Pages.Nowy.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SpecialOffer[0].SpecialOfferName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SpecialOffer[0].SpecialOfferDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SpecialOffer) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SpecialOfferName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SpecialOfferDescription)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdSpecialOffer">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdSpecialOffer">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdSpecialOffer">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
